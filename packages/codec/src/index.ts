/**
 * Package boundary for the generated ShieldFont codec.
 *
 * The canonical codec implementation is added after the Python ruleset schema
 * is established. Keeping this export explicit makes the workspace buildable
 * without inventing a second mapping contract during scaffolding.
 */
export const TOOLCHAIN_CODEC_VERSION = "0.1.0-dev";
